import 'package:flutter/material.dart';
import 'game/game_logic.dart';
import 'game_screen.dart';
import 'marble_face.dart';

class DifficultySelectionScreen extends StatelessWidget {
  final MarbleStyle selectedMarble;

  const DifficultySelectionScreen({super.key, required this.selectedMarble});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('AI ?œì´??? íƒ', style: TextStyle(color: Colors.white)),
        backgroundColor: Colors.transparent,
        iconTheme: const IconThemeData(color: Colors.white),
        elevation: 0,
      ),
      body: Container(
        decoration: const BoxDecoration(
          gradient: LinearGradient(
            begin: Alignment.topLeft,
            end: Alignment.bottomRight,
            colors: [
              Color(0xFF0A1931), // Deep dark blue
              Color(0xFF150E23), // Dark purple/black
            ],
          ),
        ),
        child: Center(
          child: Column(
            mainAxisAlignment: MainAxisAlignment.center,
            children: [
              const Text('ì»´í“¨??AI) ?œì´?„ë? ? íƒ?˜ì„¸??, style: TextStyle(fontSize: 18, color: Colors.white)),
              const SizedBox(height: 30),
              _difficultyButton(context, 'ë§¤ìš° ?¬ì?', () => _start(context, AIDifficulty.veryEasy)),
            const SizedBox(height: 20),
            _difficultyButton(context, '?¬ì?', () => _start(context, AIDifficulty.easy)),
            const SizedBox(height: 20),
            _difficultyButton(context, 'ì¤‘ê°„', () => _start(context, AIDifficulty.medium)),
            const SizedBox(height: 20),
            _difficultyButton(context, '?´ë ¤?€', () => _start(context, AIDifficulty.hard)),
            const SizedBox(height: 20),
            _difficultyButton(context, 'ë§¤ìš° ?´ë ¤?€', () => _start(context, AIDifficulty.veryHard)),
          ],
        ),
      ),
    );
  }

  Widget _difficultyButton(BuildContext context, String label, VoidCallback onTap) {
    return SizedBox(
      width: 200,
      child: ElevatedButton(
        onPressed: () {
          _showAdvanceHintDialog(context, onTap);
        },
        child: Text(label),
      ),
    );
  }

  void _start(BuildContext context, AIDifficulty d) {
    Navigator.of(context).pushReplacement(
      MaterialPageRoute(
        builder: (_) => GameScreen(
          selected: selectedMarble,
          aiDifficulty: d,
        ),
      ),
    );
  }
}

void _showAdvanceHintDialog(BuildContext context, VoidCallback onStart) {
  showDialog(
    context: context,
    builder: (ctx) => AlertDialog(
      title: const Text('?¨ê³„ ?¹ê¸‰ ?ˆë‚´'),
      content: const Text('ê²Œìž„ ???ˆë‚´: 15?ì„ ë¨¼ì? ?ì ?˜ë©´\n?¤ìŒ ?¨ê³„ë¡??¬ë¼ê°‘ë‹ˆ??\n(ë§¤ìš° ?´ë ¤?€?€ ?¹ê¸‰ ?†ìŒ)'),
      actions: [
        TextButton(
          onPressed: () => Navigator.of(ctx).pop(),
          child: const Text('?«ê¸°'),
        ),
        ElevatedButton(
          onPressed: () {
            Navigator.of(ctx).pop();
            onStart();
          },
          child: const Text('?œìž‘?˜ê¸°'),
        ),
      ],
    ),
  );
}


