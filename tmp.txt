
import 'package:flutter/material.dart';
import 'game/game_logic.dart';
import 'game_screen.dart';
import 'marble_face.dart';

class DifficultySelectionScreen extends StatelessWidget {
  final MarbleStyle selectedMarble;

  const DifficultySelectionScreen({super.key, required this.selectedMarble});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('AI ?œì´??? íƒ'),
      ),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            const Text('ì»´í“¨??AI)???œì´?„ë? ? íƒ?˜ì„¸??', style: TextStyle(fontSize: 18)),
            const SizedBox(height: 30),
            ElevatedButton(
              onPressed: () {
                Navigator.of(context).pushReplacement(
                  MaterialPageRoute(
                    builder: (_) => GameScreen(
                      selected: selectedMarble,
                      aiDifficulty: AIDifficulty.easy,
                    ),
                  ),
                );
              },
              child: const Text('?¬ì?'),
            ),
            const SizedBox(height: 20),
            ElevatedButton(
              onPressed: () {
                Navigator.of(context).pushReplacement(
                  MaterialPageRoute(
                    builder: (_) => GameScreen(
                      selected: selectedMarble,
                      aiDifficulty: AIDifficulty.medium,
                    ),
                  ),
                );
              },
              child: const Text('ì¤‘ê°„'),
            ),
            const SizedBox(height: 20),
            ElevatedButton(
              onPressed: () {
                Navigator.of(context).pushReplacement(
                  MaterialPageRoute(
                    builder: (_) => GameScreen(
                      selected: selectedMarble,
                      aiDifficulty: AIDifficulty.hard,
                    ),
                  ),
                );
              },
              child: const Text('?´ë ¤?€'),
            ),
          ],
        ),
      ),
    );
  }
}

